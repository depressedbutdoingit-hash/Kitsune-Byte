name: kitsune_byte
description: Kitsune Byte - Mobile dev platform
publish_to: 'none'
version: 3.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.2.0
  speech_to_text: ^6.6.0
  sqflite: ^2.3.0
  path: ^1.9.0
  path_provider: ^2.1.2
    uuid: ^4.2.0


flutter:
  uses-material-design: true
dependencies:
  http: ^1.2.0
  flutter_stripe: ^10.0.0  # optional, for native Stripe UI
